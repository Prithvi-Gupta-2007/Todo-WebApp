packages:
  "framer-motion": "For stunning layout transitions, ambient bubbles, and list staggering"
  "lucide-react": "For beautiful consistent iconography"
  "clsx": "For conditional tailwind classes"
  "tailwind-merge": "For merging tailwind classes safely"
  "date-fns": "For formatting created dates beautifully"

images: []

notes:
  - "Using existing API hooks generated by Orval from @workspace/api-client-react"
  - "Using Wouter for routing as defined in App.tsx"
  - "Customizing shadcn toaster by overriding CSS to top-center as requested"
